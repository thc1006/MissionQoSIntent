---
description: Leave touched code cleaner than found — small in-scope cleanups (full body later).
---

# /boyscout — leave-it-cleaner pass (STUB, Stage 0)

The full guidance is specified in a later stage. For now, follow **CLAUDE.md rule 3 (Boy Scout)**:

Within the files you already touched, tidy obvious rough edges (naming, dead code, missing types,
stale comments) — **without expanding scope** into unrelated files.
